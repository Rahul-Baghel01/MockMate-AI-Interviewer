import Link from "next/link"; import { Calendar, Clock, Download, Star } from "lucide-react"; import { formatReplayTime } from "./ReplayPlayer";
export default function ReplayHeader({ replay }: { replay: InterviewReplay }) { return <header className="rounded-[28px] border border-primary-200/15 bg-dark-300/55 p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary-200">Interview replay</p><h1 className="mt-2 text-3xl font-semibold text-white">{replay.company || "Practice"} interview</h1><div className="mt-3 flex flex-wrap gap-4 text-sm text-light-400"><span className="flex items-center gap-1"><Star size={15}/>{replay.score ?? "—"}/100</span><span className="flex items-center gap-1"><Clock size={15}/>{formatReplayTime(replay.totalDuration)}</span><span className="flex items-center gap-1"><Calendar size={15}/>{new Date(replay.completedAt).toLocaleDateString()}</span></div></div><div className="flex flex-wrap gap-2">{[["TXT","transcript&format=txt"],["Transcript PDF","transcript&format=pdf"],["Feedback PDF","feedback&format=pdf"]].map(([label,query])=><Link key={label} href={`/api/replays/${replay.interviewId}/download?type=${query}`} className="rounded-full border border-white/10 px-3 py-2 text-sm"><Download className="mr-1 inline" size={14}/>{label}</Link>)}</div></div></header>; }
