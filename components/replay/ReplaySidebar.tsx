"use client"; import { formatReplayTime } from "./ReplayPlayer";
export default function ReplaySidebar({ questions, seek }: { questions: ReplaySegment[]; seek: (time: number) => void }) { return <aside className="rounded-3xl border border-white/10 bg-dark-200/45 p-4"><h2 className="text-base text-white">Questions</h2><div className="mt-3 space-y-2">{questions.map((q,i) => <button key={`${q.timestamp}-${i}`} onClick={() => seek(q.timestamp)} className="w-full rounded-xl px-3 py-2 text-left text-sm text-light-400 hover:bg-primary-200/10"><span className="block font-semibold text-primary-100">Question {i+1} · {formatReplayTime(q.timestamp)}</span><span className="line-clamp-2">{q.text}</span></button>)}</div></aside>; }
