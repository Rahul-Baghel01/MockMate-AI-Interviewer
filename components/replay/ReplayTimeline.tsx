"use client"; import { formatReplayTime } from "./ReplayPlayer";
export default function ReplayTimeline({ replay, seek }: { replay: InterviewReplay; seek: (time: number) => void }) { return <div className="space-y-2">{replay.transcript.map((item) => <button key={item.id} onClick={() => seek(item.timestamp)} className="flex w-full gap-3 rounded-2xl border border-white/10 bg-dark-100/60 p-3 text-left hover:border-primary-200/30"><span className="text-xs text-primary-200">{formatReplayTime(item.timestamp)}</span><span><strong className="text-sm text-white">{item.role === "assistant" ? "AI" : "Answer"}</strong><span className="mt-1 line-clamp-2 block text-sm text-light-400">{item.content}</span></span></button>)}</div>; }
